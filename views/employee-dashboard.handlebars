
/*{{#if tasks.length}}
<section>
  <h2>Your Tasks</h2>
  <ol>
    {{#each tasks as |task|}}
    <li>
      {{> task-info task}}
      <button type="check" id="delete-btn">Task Complete</button>
    </li>
    {{/each}}
  </ol>
</section>
{{/if}}*/

<h1>This is employee page</h1>