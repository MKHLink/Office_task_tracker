{{#if tasks.length}}
<section>
  <h2>Your Tasks</h2>
  <ol>
    {{#each tasks as |task|}}
    <li>
      {{> task-info task}}
      <a href="/dashboard/edit/{{task.id}}" class="edit-link">Edit Task</a>
    </li>
    {{/each}}
  </ol>
</section>
{{/if}}

